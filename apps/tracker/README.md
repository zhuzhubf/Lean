# Tracker Service (Placeholder)

`apps/tracker/upstream` is reserved for the `nof1-tracker` fork submodule. The tracker is optional for Phase 1 but will be used for analytics and replay later.

Add your fork with:

```bash
git submodule add https://github.com/<GITHUB_OWNER>/nof1-tracker-fork.git apps/tracker/upstream
```
